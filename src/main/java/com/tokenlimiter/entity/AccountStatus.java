package com.tokenlimiter.entity;

/**
 * User account status.
 */
public enum AccountStatus {

    /** Account is active; operations are allowed. */
    ACTIVE,

    /** Account is suspended; consumption is not allowed. */
    SUSPENDED
}
